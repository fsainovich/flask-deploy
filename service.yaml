apiVersion: v1
kind: Service 
metadata:
  name: flask-env-var-service
spec:
  selector: 
    app: flask-env-var
  ports:
    - protocol: TCP
      port: 80 # Porta do Service
      targetPort: 5000 # Porta dos Pods